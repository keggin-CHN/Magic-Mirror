tinyface >= 1.0.1
bottle >= 0.13.2
nuitka >= 2.5.9
async-tasks-python >= 1.0.0
# tinyface 1.0.1 requires numpy>=2.1.2; opencv-python 4.12+ also requires numpy>=2 on Python>=3.9
opencv-python >= 4.12.0.88, < 5.0.0
numpy >= 2.1.2, < 3.0.0
# TinyFace declares a dependency on the CPU ``onnxruntime`` distribution. The
# Windows release workflow replaces it with exactly one GPU runtime after the
# base dependencies are installed; DirectML and CUDA wheels cannot coexist
# because both own the same ``onnxruntime`` Python package.
# See scripts/install-windows-ort.ps1.
#
# Non-Windows builds keep the regular CPU runtime.
onnxruntime >= 1.16.0 ; sys_platform != "win32"
