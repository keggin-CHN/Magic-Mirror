tinyface >= 1.0.1
fastapi >= 0.110.0, < 0.111.0
uvicorn >= 0.29.0
python-multipart >= 0.0.9
av >= 12.0.0
packaging >= 24.0
async-tasks-python >= 1.0.0
opencv-python >= 4.12.0.88, < 5.0.0
numpy >= 2.1.2, < 3.0.0
# GPU acceleration support (DirectML for Windows, compatible with NVIDIA/AMD/Intel GPUs)
onnxruntime-directml >= 1.16.0 ; sys_platform == "win32"
# Fallback to CPU-only ONNX Runtime for non-Windows platforms
onnxruntime >= 1.16.0 ; sys_platform != "win32"
